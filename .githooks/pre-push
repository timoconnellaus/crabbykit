#!/usr/bin/env bash
# Run the full test + build suite before publishing commits to origin.
# Bypass with `git push --no-verify` in emergencies.

set -e

echo "[pre-push] test..."
bun run test

echo "[pre-push] build..."
bun run build
