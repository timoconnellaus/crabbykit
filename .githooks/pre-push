#!/usr/bin/env bash
# Catch real bugs before they hit remote. CI owns coverage thresholds + build + Docker.
# Bypass with `git push --no-verify` in emergencies.

set -e

echo "[pre-push] test (serial — parallel fan-out starves workerd of loopback ports)..."
bun run test:serial
