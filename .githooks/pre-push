#!/usr/bin/env bash
# Run the full test (with coverage) + build suite before publishing commits.
# Bypass with `git push --no-verify` in emergencies.

set -e

echo "[pre-push] test:coverage (serial — avoids workerd port contention)..."
bun run test:coverage:serial

echo "[pre-push] build..."
bun run build
