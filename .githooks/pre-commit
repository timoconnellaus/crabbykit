#!/usr/bin/env bash
# Fast local gate: lint + typecheck. Tests run on pre-push; CI owns coverage + build.
# Bypass with `git commit --no-verify` in emergencies.

set -e

echo "[pre-commit] biome check + dep graph..."
bun run lint

echo "[pre-commit] typecheck..."
bun run typecheck
